<div id="<%= dom_id book %>">
  <p>
    <strong><%= t('books.book.title') %>:</strong>
    <%= book.title %>
  </p>

  <p>
    <strong><%= t('books.book.memo') %>:</strong>
    <%= book.memo %>
  </p>

  <p>
    <strong><%= t('books.book.author') %>:</strong>
    <%= book.author %>
  </p>

  <p>
    <strong><%= t('books.book.picture') %>:</strong>
    <%= image_tag(book.picture_url) if book.picture.present? %>
  </p>

</div>
