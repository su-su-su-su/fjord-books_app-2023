<h1><%= t('shared.edit.editing', name: Book.model_name.human) %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('shared.back_to_show', name: Book.model_name.human), @book %> |
  <%= link_to t('shared.back', name: Book.model_name.human), books_path %>
</nav>
